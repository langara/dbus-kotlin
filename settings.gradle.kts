pluginManagement {
    repositories {
        jcenter()
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "dbus-kotlin" // should always be kept the same as Konf.name

include(":dbus-kotlin")
